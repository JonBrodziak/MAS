notifications:
  email: false

# Enable C++ support
language: cpp


# Compiler selection
compiler:
  - gcc
 
# Build steps
script:
  - cd third_party
  - git clone https://github.com/msupernaw/ATL.git
  - cd ..
  - g++ -std=c++0x -O3 main.cpp -o mas.x
